The attribute names are searched for the transferred string.
The hits are saved with their values in an object.
The search string + a space is removed, the country code remains:
{"Name Product EN": "Book"} -> {"EN": "book"}
